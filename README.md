# label-helper
Node app for labelling data

## Usage

To use, just clone the repository and run `npm install`, and then run the `app.js` file (for example by running `node app.js`)

The app is still in an early stage of the development, but you should be able to see a nice looking graph.
